#ifndef MATH_H
#define MATH_H

#include "stddef.h"
#include "types.h"

f64 sqrt(f64 x);
f64 pow(f64 base, f64 exp);
int abs(int x);
void srand(int32 seed);
int rand();
int randrange(int min, int max);
f64 pow(f64 base, f64 exp);

#endif